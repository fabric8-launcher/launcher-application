#!/bin/sh

if [ $RELEASE ]; then
  APP_VERSION=$RELEASE
else
  APP_VERSION=1.0.0.Alpha1
fi

if [ $MAVEN_MIRROR_URL ]; then
  REPO=$MAVEN_MIRROR_URL
else
  REPO=repository.jboss.org/nexus/content/repositories/public
fi

curl -o /deployments/generator-swarm.jar -L https://$REPO/org/obsidiantoaster/generator-backend/$APP_VERSION/generator-backend-$APP_VERSION-swarm.jar
